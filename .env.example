# ===========================================
# Example file for environment variables
# Copy to .env and fill in your values
# ===========================================

# Database Configuration
DB_HOST=postgres-db
DB_PORT=5432
DB_USER=<teu_utilizador>
DB_PASSWORD=<tua_password_segura>
DB_NAME=apidb

# Azure Functions
AzureWebJobsStorage=
FUNCTIONS_WORKER_RUNTIME=node
